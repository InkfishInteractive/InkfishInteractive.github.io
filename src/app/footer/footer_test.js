'use strict';

describe('inkfish.hero module', function() {

  beforeEach(module('inkfish.hero'));

  describe('hero controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var heroCtrl = $controller('HeroCtrl');
      expect(heroCtrl).toBeDefined();
    }));

  });
});
